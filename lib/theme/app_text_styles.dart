import 'package:flutter/material.dart';

abstract class AppTextStyles {
  // Quote TextStyle.
  static const quoteText =
      TextStyle(fontWeight: FontWeight.w500, fontSize: 25, color: Colors.black);

  // Quote TextStyle.
  static const quoteAuthor =
      TextStyle(fontWeight: FontWeight.w500, fontSize: 20, color: Colors.black);

  // Quote TextStyle.
  static const bodyListTile =
      TextStyle(fontWeight: FontWeight.w400, fontSize: 14, color: Colors.black);

  // ListTile SubTitle TextStyle.
  static const subtitleListTile =
      TextStyle(fontWeight: FontWeight.w600, fontSize: 14, color: Colors.black);
}
